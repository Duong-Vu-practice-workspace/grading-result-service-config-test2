apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    app: result-service
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: result-service
